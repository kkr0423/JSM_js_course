// Falsy Values
/**
 * false
 * 0
 * null
 * undefined
 * NaN
 * "", ''
 */

//Truthy Values
// All values that are not falsy.
const value = "";

if(true) {
    console.log(`${value} is TRUTHY`);
} else {
    console.log(`${value} is FALSY`);
}